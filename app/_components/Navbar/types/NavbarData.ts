export type NavbarData = {
  logo: string;
  menuList: App.NavbarMenu[];
  ctaButton: App.CtaButton;
  justifyContent: string;
};

export type FeaturedData = {
  imgUrl: string;
  title: string;
  subTitle: string;
  blogUrl: string;
};
