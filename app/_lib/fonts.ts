import { Cinzel, Cinzel_Decorative } from "next/font/google";

export const cinzel = Cinzel({ weight: "700", subsets: ["latin"] })
export const cinzelDecorative = Cinzel_Decorative({ weight: "700", subsets: ["latin"] });