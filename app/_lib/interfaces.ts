interface Feature {
  id: string;
  title: string;
  description: string;
}

interface Case {
  id: string;
  firstImage: string;
  secondImage: string;
}

interface Review {
  id: string;
  comment: string;
  author: string;
  rate: number;
}